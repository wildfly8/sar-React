# Project Profile — sar (Securities Analysis & Research)

Generated by brownfield scan. Facts only — derived from the repository on disk.

## Tech Stack

| Category | Detected |
|----------|----------|
| **Primary language** | JavaScript (CRA). App `src/`: `.jsx` ~57%, `.js` ~39%, `.css` ~4% |
| **TypeScript / Python** | Not detected in application source |
| **Frontend** | React 16.8, React DOM, react-router-dom 5, Create React App (`react-scripts` 3.4.1) |
| **UI** | Bootstrap 4, react-bootstrap, react-bootstrap-table-next (+ editor) |
| **Charts / misc deps** | chart.js, react-chartjs-2 declared; **no chart imports under `src/`** |
| **Backend** | **Not in this repo.** HTTP API at `http://localhost:9090` (`src/api/index.js`); CRA `"proxy": "http://localhost:9090/"` |
| **Database / ORM** | Not detected |
| **Auth** | **None** — Login/Profile/Okta removed from source and dependencies |
| **Testing** | `npm test` → `react-scripts test` (Jest). **No test files present** |
| **CI/CD** | Not detected |
| **Package manager** | npm (`package-lock.json`) |
| **Build** | `npm start` (OpenSSL legacy provider), `npm run build`, `npm test`, `npm run eject` |

## Architecture

- **Pattern**: Monolith SPA (single CRA frontend)
- **Entry**: `src/index.js` → `App` → `BrowserRouter` + feature `Route`s
- **State**: Local `useState` / `useEffect`; `MyContext` for macro indices, TYT/VIX, auth stubs; some `sessionStorage` caching
- **API**: `myFetcher` (`fetch` → JSON) against `${SERVER_URL}/${VERSION}/api/...`

### Feature routes

| Route | Component |
|-------|-----------|
| `/` | WatchList |
| `/login` | Login (disabled placeholder) |
| `/macro-econ` | MacroEcon |
| `/company-analysis` | CompanyAnalysis |
| `/rating-enforce` | RatingEnforce |
| `/screening` | Screening |
| `/valuation` | Valuation |
| `/px-target` | PxTarget |
| `/profile` | Profile |

## Module Map

| Module | Path | Purpose |
|--------|------|---------|
| App shell | `src/App.js`, `src/App.css`, `src/index.js` | Router, context, global Bootstrap CSS |
| API client | `src/api/index.js` | `SERVER_URL`, `VERSION`, `myFetcher` |
| Constants / utils | `src/*Constants.js`, `src/MyUtil.js`, `src/polyfills.js` | Sector/exchange/macro constants, helpers |
| Feature UI | `src/components/*.jsx` | Page + table components per feature (no Login/Profile) |
| Static | `public/` | HTML shell, assets, manifest |
| Spec Kit | `.specify/`, `.cursor/skills/` | SDD tooling (not app runtime) |

## Conventions

| Area | Detected |
|------|----------|
| **Component files** | PascalCase `.jsx` under `src/components/` |
| **Root modules** | Descriptive `.js` (`MyUtil.js`, `api/index.js`) |
| **Components** | Functional; frequent `React.memo`; tables as separate `*Table.jsx` |
| **Branch / commit style** | Not detected (no `.git` history in workspace) |
| **Tests** | None present |
| **Docs** | Root `README.md` is CRA boilerplate |

## Existing Governance

| Artifact | Status |
|----------|--------|
| `.specify/` | Present |
| `README.md` / `.gitignore` | Present |
| ESLint | `package.json` → `extends: react-app` only |
| CONTRIBUTING / ARCHITECTURE / ADR / AGENTS.md | Absent |
| `.editorconfig` / Prettier / CI workflows | Absent |
| `specs/` | Present — migrated features `001`–`007` |
| `.specify/domain/` | Absent |

## Recommendations

- Constitution and templates MUST describe CRA React 16 SPA + external `:9090` API — not Next.js, Vercel, or `domain:*` Python gates.
- Feature work typically touches `src/components/`, optionally `src/api/`, constants/utils, and `App.js` routes/context.
- Backend API contract changes are out of band for this repo unless a companion API repo is in scope.
